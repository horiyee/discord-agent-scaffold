[project]
name = "bot"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "horiyee", email = "horiyee.ka@gmail.com" }
]
requires-python = ">=3.14"
dependencies = []

[project.scripts]
bot = "bot:main"

[build-system]
requires = ["uv_build>=0.10.5,<0.11.0"]
build-backend = "uv_build"
